apiVersion: apps/v1
kind: Deployment
metadata:
  name: tone-registry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tone-registry
  template:
    metadata:
      labels:
        app: tone-registry
    spec:
      containers:
        - name: tone-registry
          image: tone-registry
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: manifests-volume
              mountPath: /manifests
      volumes:
        - name: manifests-volume
          configMap:
            name: registry-manifests
